export function Loader() {
  return (
    <h1
      style={{
        textAlign: "center",
        fontWeight: "lighter",
        marginBottom: "20px",
      }}
    >
      Loading...
    </h1>
  );
}
